language: minimal

services: docker

env:
  global:
    - COMPOSE_PROJECT_NAME=articlestore

install:
  - ENV=dev make build
  - ENV=prod make build

script:
  - ENV=dev .travis/smoke-test.sh
  - ENV=prod .travis/smoke-test.sh
